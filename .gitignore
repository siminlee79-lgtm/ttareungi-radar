.DS_Store
Thumbs.db
node_modules/
www/
.wrangler/
.dev.vars
config.local.js
android/app/google-services.json
android/.idea/
android/gradle/gradle-daemon-jvm.properties
.android-user/
.gradle-user/

# Firebase service account keys — NEVER commit
*-firebase-adminsdk-*.json
firebase-service-account*.json
raw-data/**/*.csv
raw-data/**/*.xlsx
raw-data/**/*.xls
raw-data/**/*.zip

# wrangler download dumps secrets in plain text — never commit
wrangler.toml

# Upload key and its passwords — never commit
keystore.properties
*.jks
*.keystore
upload_certificate.pem

# Play Console 업로드용 사본 (원본은 assets/ 에 있음)
play-assets/
